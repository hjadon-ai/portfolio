# REST routes

Keep Express route handlers here. Update the OpenAPI definition and Postman collection whenever an endpoint changes. Daily Priorities derives ownership from the session and uses atomic single-document mutations for its three-item limit.
