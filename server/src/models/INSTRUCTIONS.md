# MongoDB models

Keep Mongoose models here. Add or change a collection only after documenting its purpose and fields in `server/design`. F006 Finance collection fields and indexes are recorded in `server/design/mongodb-collections.md`.
